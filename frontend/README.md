# Aria Fashion — Frontend

React 19 + TypeScript SPA (Vite, React Router, TanStack Query, Zustand, Tailwind CSS v4) for the Aria Fashion boutique site. Talks to the Laravel API in `../FashionWeb` over `VITE_API_URL`.

See the [project README](../README.md) for full setup instructions.
