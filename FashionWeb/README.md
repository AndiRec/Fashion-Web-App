# Aria Fashion — API

Laravel 12 JSON API for the Aria Fashion boutique site. Serves products, cart, wishlist, checkout, orders, addresses, and auth (Laravel Sanctum) to the React SPA in `../frontend`.

See the [project README](../README.md) for full setup instructions.
